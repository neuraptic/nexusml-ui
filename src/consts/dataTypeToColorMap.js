import { colors } from './colors';

export const dataTypeToColorMap = {
	input: colors.taskInput,
	metadata: colors.taskMetaData,
	output: colors.taskOutput,
	aioutput: colors.taskTarget,
	group: colors.taskGroups,
};
